# ClipForge Secure Configuration Template
# Copy this file to .env and configure with your values

# ========================================
# REQUIRED SETTINGS
# ========================================

# Security Configuration (REQUIRED)
# IMPORTANT: Generate a secure JWT secret (minimum 32 characters)
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
CLIPFORGE_JWT_SECRET=your-secure-jwt-secret-here-minimum-32-chars

# Plex Configuration (REQUIRED)
CLIPFORGE_PLEX_SERVER_NAME=YourPlexServer

# ========================================
# OPTIONAL OVERRIDES
# Remove any line to use the default from constants.py
# ========================================

# Security Settings
CLIPFORGE_SECURE_COOKIES=false  # Set to true in production with HTTPS

# Storage Overrides (defaults in constants.py)
# CLIPFORGE_MAX_CLIP_DURATION=600  # Override default 10 minutes (600 seconds)
# CLIPFORGE_MAX_CLIP_SIZE_MB=500   # Override default 500 MB

# User Limits (defaults in constants.py)
# CLIPFORGE_USER_VIDEO_LIMIT=60    # Override default 60 videos per user

# Server Override (defaults in constants.py)
# CLIPFORGE_HOST=0.0.0.0
# CLIPFORGE_PORT=8002

# Storage Path Override (defaults in constants.py)
# CLIPFORGE_CLIPS_STORAGE_PATH=static/clips
# CLIPFORGE_CLIP_RETENTION_DAYS=7